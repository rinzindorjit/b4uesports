// Re-export all route handlers from individual files
export { default as piPriceRouter } from './pi-price.js';

// Export the registerRoutes function from the main routes file
export { registerRoutes } from '../routes.js';